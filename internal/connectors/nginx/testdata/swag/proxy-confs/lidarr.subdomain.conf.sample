## A shipped sample: the *.subdomain.conf glob must not pick it up.
server {
	listen 443 ssl;
	server_name lidarr.*;
	location / {
		set $upstream_app lidarr;
		set $upstream_port 8686;
		set $upstream_proto http;
		proxy_pass $upstream_proto://$upstream_app:$upstream_port;
	}
}
